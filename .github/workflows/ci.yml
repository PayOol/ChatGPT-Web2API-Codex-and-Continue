name: Windows distribution
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:
    inputs:
      install:
        description: Install the full managed distribution on a fresh runner
        type: boolean
        default: false
permissions:
  contents: read
jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.14.3'
      - uses: actions/setup-node@v4
        with:
          node-version: '24.14.0'
      - run: python -m pip install -e ".[dev]"
      - run: ruff check src/ tests/
      - run: python -m pytest -q -m "not e2e"
      - run: python installer/build_release.py --output dist
      - uses: actions/upload-artifact@v4
        with:
          name: Windows-installer
          path: dist/*
  secret-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install and run Gitleaks
        run: |
          curl -fsSL https://github.com/gitleaks/gitleaks/releases/download/v8.21.2/gitleaks_8.21.2_linux_x64.tar.gz -o /tmp/gitleaks.tar.gz
          tar -xzf /tmp/gitleaks.tar.gz -C /tmp gitleaks
          /tmp/gitleaks detect --source . --redact
  installation:
    if: github.event_name == 'workflow_dispatch' && inputs.install
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.14.3'
      - name: Build the executable to test
        run: python installer/build_release.py --output dist
      - name: Install without opening the editor or creating shortcuts
        shell: powershell
        run: |
          $exe=(Get-ChildItem dist\Web2API-Continue-Setup-*.exe).FullName
          & $exe --target continue --root "$env:RUNNER_TEMP\Web2API test" --no-launch --no-shortcuts
          if ($LASTEXITCODE -ne 0) { throw 'Installer failed' }
          $log=Get-Content "$env:RUNNER_TEMP\Web2API test\logs\install.log" -Raw
          if ([regex]::Matches($log,'ETAPE \[').Count -ne 21 -or $log -notmatch '21/21 etapes \(100%\)') { throw 'Incomplete progress transcript' }
      - name: Inspect all five live MCP catalogs
        shell: powershell
        run: |
          $root="$env:RUNNER_TEMP\Web2API test"
          & "$root\venv\Scripts\python.exe" "$root\app\installer\doctor.py" --root $root --offline --check-tools
          if ($LASTEXITCODE -ne 0) { throw 'Tool diagnostic failed' }
      - name: Launch the actual editor and load the Continue model without launcher environment
        run: python tests/editor_runtime.py --root "${{ runner.temp }}/Web2API test"
      - uses: actions/upload-artifact@v4
        with:
          name: Installer-tested-on-Windows
          path: dist/*
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: Installation-progress-log
          path: |
            ${{ runner.temp }}/Web2API test/logs/install.log
            ${{ runner.temp }}/Web2API test/logs/editor-runtime.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: Normal-editor-logs
          path: C:/Users/runneradmin/AppData/Roaming/Code/logs/**
  installation-codex:
    if: github.event_name == 'workflow_dispatch' && inputs.install
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.14.3'
      - name: Build the executable to test
        run: python installer/build_release.py --output dist
      - name: Isolate the native Codex profile for this runner
        shell: pwsh
        run: |
          "CODEX_HOME=$env:RUNNER_TEMP\codex-profile" >> $env:GITHUB_ENV
      - name: Install the Codex route without Store UI or shortcuts
        shell: powershell
        run: |
          $exe=(Get-ChildItem dist\Web2API-Continue-Setup-*.exe).FullName
          & $exe --target codex --root "$env:RUNNER_TEMP\Web2API Codex test" --no-launch --no-shortcuts --skip-desktop
          if ($LASTEXITCODE -ne 0) { throw 'Codex target installation failed' }
      - name: Verify the model in the real Codex App Server
        run: python tests/codex_runtime.py --root "${{ runner.temp }}/Web2API Codex test"
      - name: Verify selective removal
        shell: powershell
        run: |
          $root="$env:RUNNER_TEMP\Web2API Codex test"
          & "$root\venv\Scripts\python.exe" "$root\app\installer\configure_codex.py" --root $root --detach
          if ($LASTEXITCODE -ne 0) { throw 'Selective Codex removal failed' }
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: Codex-installation-proof
          path: |
            ${{ runner.temp }}/Web2API Codex test/logs/**
